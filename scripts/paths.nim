import os


const box2dRoot* = (
    currentSourcePath.parentDir.parentDir / 
    "src" / 
    "box2d" / 
    "private" / 
    "box2d"
)


const libraryRoot* = (
     currentSourcePath.parentDir.parentDir / 
     "src" / 
     "box2d"
)
